<%= form_with(model: cord, local: true) do |form| %>
  <% if cord.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cord.errors.count, "error") %> prohibited this cord from being saved:</h2>

      <ul>
      <% cord.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :racquet_id %>
    <%= form.text_field :racquet_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :colour %>
    <%= form.text_field :colour %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
