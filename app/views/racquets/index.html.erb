<p id="notice"><%= notice %></p>

<h1>Racquets</h1>

<table>
  <thead>
    <tr>
      <th>Brand</th>
      <th>Level</th>
      <th>Weight</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @racquets.each do |racquet| %>
      <tr>
        <td><%= racquet.brand %></td>
        <td><%= racquet.level %></td>
        <td><%= racquet.weight %></td>
        <td><%= link_to 'Show', racquet %></td>
        <td><%= link_to 'Edit', edit_racquet_path(racquet) %></td>
        <td><%= link_to 'Destroy', racquet, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Racquet', new_racquet_path %>
